local function espControl()local espEnabled=false local espHolders={}local espConnections={}local function clearESP()for _,holder in pairs(espHolders)do pcall(function()holder:Destroy()end)end espHolders={}for _,conn in pairs(espConnections)do pcall(function()conn:Disconnect()end)end espConnections={}end local function createESP(player)if player==game.Players.LocalPlayer then return end local char=player.Character if not char then return end local root=char:FindFirstChild("HumanoidRootPart")if not root then return end local holder=Instance.new("Folder")holder.Name=player.Name.."_ESP"holder.Parent=game:GetService("CoreGui")local box=Instance.new("BoxHandleAdornment")box.Size=root.Size box.Adornee=root box.AlwaysOnTop=true box.ZIndex=10 box.Transparency=0.5 box.Color3=player.TeamColor or Color3.fromRGB(255,255,255)box.Parent=holder local head=char:FindFirstChild("Head")if head then local billboard=Instance.new("BillboardGui")billboard.Adornee=head billboard.Size=UDim2.new(0,150,0,50)billboard.StudsOffset=Vector3.new(0,2,0)billboard.AlwaysOnTop=true billboard.Parent=holder local label=Instance.new("TextLabel")label.Size=UDim2.new(1,0,1,0)label.BackgroundTransparency=1 label.Text=player.Name label.TextColor3=Color3.fromRGB(255,255,255)label.Font=Enum.Font.GothamBold label.TextSize=20 label.TextStrokeTransparency=0.5 label.Parent=billboard end table.insert(espHolders,holder)local conn1=player.CharacterAdded:Connect(function()pcall(function()for _,h in pairs(espHolders)do if h.Name==player.Name.."_ESP"then h:Destroy()end end end)task.wait(0.5)if espEnabled then createESP(player)end end)local conn2=player:GetPropertyChangedSignal("TeamColor"):Connect(function()pcall(function()for _,h in pairs(espHolders)do if h.Name==player.Name.."_ESP"then for _,b in pairs(h:GetDescendants())do if b:IsA("BoxHandleAdornment")then b.Color3=player.TeamColor or Color3.fromRGB(255,255,255)end end end end end)end)table.insert(espConnections,conn1)table.insert(espConnections,conn2)end local function start()if espEnabled then return end espEnabled=true for _,player in pairs(game.Players:GetPlayers())do createESP(player)end local conn=game.Players.PlayerAdded:Connect(createESP)table.insert(espConnections,conn)end local function stop()espEnabled=false clearESP()end return{start=start,stop=stop}end local api=espControl()if api then getgenv().ESPControl=api end
